# 📋 Краткая инструкция по тестированию

## ⚡ Быстрый старт (5 шагов)

### 1️⃣ Подготовка окружения
```bash
# Активировать виртуальное окружение
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# Установить зависимости (если еще не установлены)
pip install -r requirements.txt
```

### 2️⃣ Настройка .env
```bash
# Скопировать пример
cp .env.example .env

# Отредактировать .env (указать TELEGRAM_BOT_TOKEN)
nano .env  # или любой редактор
```

**Минимальные настройки:**
```env
TELEGRAM_BOT_TOKEN=your_token_here
DATABASE_URL=postgresql://trivia_user:trivia_password@localhost:5432/trivia_bot
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2
```

### 3️⃣ Создание БД и тестовых данных
```bash
# Создать таблицы
python scripts/create_tables.py

# Добавить тестовые данные (темы, вопросы, боты)
python scripts/add_test_data.py

# Проверить статус
python scripts/check_status.py
```

**Ожидаемый результат:**
- ✅ 5 тем создано
- ✅ 12+ вопросов создано
- ✅ 10 ботов создано

### 4️⃣ Запуск сервисов

**Откройте 3 терминала:**

**Терминал 1 - Telegram Bot:**
```bash
source venv/bin/activate
python main.py
```

**Терминал 2 - Celery Worker:**
```bash
source venv/bin/activate
celery -A tasks.celery_app worker --loglevel=info
```

**Терминал 3 - Celery Beat:**
```bash
source venv/bin/activate
celery -A tasks.celery_app beat --loglevel=info
```

### 5️⃣ Тестирование

#### A. Базовый тест бота
1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. ✅ Должно появиться меню

#### B. Создание тестовой игры
```bash
# Узнайте ваш Telegram ID (отправьте /start, ID будет в логах бота)
# Или используйте @userinfobot в Telegram

python scripts/create_test_game.py --telegram-id YOUR_TELEGRAM_ID
```

**В Telegram:**
1. ✅ Должно прийти: "🎮 Игра начинается!"
2. ✅ Должен прийти первый вопрос с кнопками
3. Нажмите на кнопку ответа
4. ✅ Должно появиться уведомление о правильности

#### C. Проверка автоматики
- Подождите 20 секунд
- ✅ Должен прийти следующий вопрос автоматически
- ✅ Боты отвечают автоматически (проверьте логи Celery)

---

## 🔍 Проверка работы

### Логи
```bash
# Логи бота
tail -f logs/trivia_bot.log

# Логи Celery
tail -f logs/celery_worker.log
```

### Статус системы
```bash
python scripts/check_status.py
```

### Проверка БД
```bash
psql -U trivia_user -d trivia_bot -c "SELECT COUNT(*) FROM questions;"
psql -U trivia_user -d trivia_bot -c "SELECT COUNT(*) FROM games WHERE status = 'in_progress';"
```

---

## ✅ Чек-лист тестирования

### Базовый функционал
- [ ] Бот отвечает на `/start`
- [ ] Меню отображается
- [ ] Кнопки работают
- [ ] Рейтинг отображается
- [ ] Статистика отображается

### Игровой процесс
- [ ] Тестовая игра создается
- [ ] Вопросы отправляются в ЛС
- [ ] Inline-кнопки работают
- [ ] Ответы обрабатываются
- [ ] Следующие вопросы отправляются автоматически
- [ ] Боты отвечают автоматически
- [ ] Результаты раунда отправляются
- [ ] Выбывание работает

---

## 🐛 Частые проблемы

| Проблема | Решение |
|----------|---------|
| Бот не отвечает | Проверьте `TELEGRAM_BOT_TOKEN` в `.env` |
| Вопросы не отправляются | Проверьте, что Celery worker запущен |
| "User not found" | Отправьте `/start` боту сначала |
| "No questions" | Запустите `python scripts/add_test_data.py` |
| Redis не работает | `sudo systemctl start redis-server` |

---

## 📚 Подробная документация

- **Полное руководство:** `TESTING_GUIDE.md`
- **Быстрый старт:** `QUICK_START_TESTING.md`
- **Скрипты:** `scripts/README.md`

---

## 🎯 Что тестировать дальше

После успешного базового тестирования:
1. ✅ Быстрая игра (нужно несколько аккаунтов)
2. ✅ Голосование (3-9 игроков)
3. ✅ Досрочная победа в финале
4. ✅ Тай-брейк при равенстве
