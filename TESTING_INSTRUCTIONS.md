# Инструкция по тестированию игрового процесса

## Шаг 1: Добавление тестовых данных

### 1.1. Подключитесь к серверу (если еще не подключены)

```bash
ssh user@your-server
cd ~/trivia-bot
```

### 1.2. Активируйте виртуальное окружение

```bash
source venv/bin/activate
```

### 1.3. Добавьте тестовые данные (темы, вопросы, боты)

```bash
python scripts/add_test_data.py
```

**Ожидаемый результат:**
```
[INFO] Starting test data creation...
[INFO] Created theme: Кино
[INFO] Created theme: Наука
[INFO] Created theme: Спорт
[INFO] Created theme: География
[INFO] Created theme: История
[INFO] Created question: Какой актер сыграл Тони Старка в киновселенной Marvel?...
[INFO] Created bot: Bot_Alpha (novice)
...
==================================================
Test data creation completed!
Themes created: 5
Questions created: 12
Bots created: 10
==================================================
```

### 1.4. Проверьте статус системы

```bash
python scripts/check_status.py
```

**Ожидаемый результат:**
```
SYSTEM STATUS
==================================================
👥 Users: Total: 11, Real users: 1, Bots: 10
📚 Themes: 5
❓ Questions: Total: 12, Approved: 12
🎮 Games: Total: 0, Active: 0, Finished: 0
```

---

## Шаг 2: Узнайте ваш Telegram ID

У вас есть два способа:

### Способ 1: Через бота @userinfobot
1. Откройте Telegram
2. Найдите бота `@userinfobot`
3. Отправьте ему `/start`
4. Он покажет ваш ID (например: `320889576`)

### Способ 2: Из логов бота
1. В tmux сессии переключитесь на панель с ботом (Ctrl+B, затем стрелка влево)
2. Найдите в логах строку с вашим username при отправке `/start`
3. Там будет ваш `telegram_id`

**Пример из логов:**
```
User 320889576 (spiritwalker84) started the bot
```
Ваш ID: `320889576`

---

## Шаг 3: Создание тестовой игры

### 3.1. Откройте новый терминал (или новую панель в tmux)

**Вариант A: Новый терминал SSH**
```bash
ssh user@your-server
cd ~/trivia-bot
source venv/bin/activate
```

**Вариант B: Новая панель в tmux**
- В tmux нажмите `Ctrl+B`, затем `c` (создать новое окно)
- Или просто откройте новый SSH терминал

### 3.2. Создайте тестовую игру

**Замените `YOUR_TELEGRAM_ID` на ваш реальный ID:**

```bash
python scripts/create_test_game.py --telegram-id YOUR_TELEGRAM_ID --type training
```

**Пример:**
```bash
python scripts/create_test_game.py --telegram-id 320889576 --type training
```

**Ожидаемый результат:**
```
[INFO] Created training game 1 with 10 players
[INFO] Starting game...
[INFO] Game 1 started! Check your Telegram for questions.
```

---

## Шаг 4: Тестирование в Telegram

### 4.1. Проверьте Telegram

После выполнения команды из шага 3.2:

1. ✅ **Должно прийти сообщение** "🎮 Игра начинается!"
2. ✅ **Должен прийти первый вопрос** с кнопками A, B, C, D
3. ✅ **Вопрос должен содержать:**
   - Текст вопроса
   - 4 варианта ответа (A, B, C, D)
   - Таймер (20 секунд)

### 4.2. Ответьте на вопрос

1. Нажмите на любую кнопку (A, B, C или D)
2. ✅ **Должно появиться уведомление:**
   - "✅ Правильно!" (если ответ правильный)
   - "❌ Неправильно. Правильный ответ: X" (если неправильный)

### 4.3. Проверьте автоматику

1. **Подождите 20 секунд** после отправки вопроса
2. ✅ **Должен автоматически прийти следующий вопрос**
3. ✅ **Боты должны отвечать автоматически** (проверьте логи Celery worker)

### 4.4. Проверьте логи

**В tmux сессии переключитесь на панель с Celery worker:**
- `Ctrl+B`, затем стрелка вправо (или вниз для панели 2)

**Вы должны увидеть:**
```
[INFO] Bot Bot_Alpha answered question 1: A (correct: True)
[INFO] Bot Bot_Beta answered question 1: B (correct: True)
...
```

### 4.5. Дождитесь завершения раунда

1. После 10 вопросов раунд должен завершиться
2. ✅ **Должны прийти результаты раунда** с таблицей мест
3. ✅ **Должен быть указан выбывший игрок**

---

## Шаг 5: Проверка статуса игры

### 5.1. Проверьте статус в БД

```bash
python scripts/check_status.py
```

**Ожидаемый результат:**
```
SYSTEM STATUS
==================================================
👥 Users: Total: 11, Real users: 1, Bots: 10
📚 Themes: 5
❓ Questions: Total: 12, Approved: 12
🎮 Games: Total: 1, Active: 1, Finished: 0
```

### 5.2. Проверьте игру напрямую в БД (опционально)

```bash
psql -U trivia_user -d trivia_bot
```

**В psql выполните:**
```sql
-- Проверить игры
SELECT id, game_type, status, current_round FROM games;

-- Проверить игроков
SELECT user_id, is_bot, total_score, is_eliminated 
FROM game_players 
WHERE game_id = 1;

-- Проверить ответы
SELECT COUNT(*) as total_answers FROM answers WHERE game_id = 1;

-- Выйти
\q
```

---

## Возможные проблемы и решения

### Проблема: "User with telegram_id X not found"

**Решение:**
1. Отправьте `/start` боту в Telegram
2. Проверьте, что бот запущен (панель 0 в tmux)
3. Повторите создание игры

### Проблема: "Only X bots available, need Y"

**Решение:**
```bash
python scripts/add_test_data.py
```

### Проблема: "No questions found"

**Решение:**
```bash
python scripts/add_test_data.py
```

### Проблема: Вопросы не отправляются

**Решение:**
1. Проверьте, что Celery worker запущен (панель 1 в tmux)
2. Проверьте, что Celery beat запущен (панель 2 в tmux)
3. Проверьте логи Celery worker на ошибки

### Проблема: Боты не отвечают

**Решение:**
1. Проверьте логи Celery worker
2. Убедитесь, что Redis работает: `redis-cli ping` (должен ответить `PONG`)
3. Перезапустите Celery worker

---

## Что проверить после тестирования

- [ ] Бот отвечает на команды
- [ ] Вопросы отправляются в ЛС
- [ ] Inline-кнопки работают
- [ ] Ответы обрабатываются
- [ ] Боты отвечают автоматически
- [ ] Следующие вопросы отправляются автоматически
- [ ] Результаты раунда отправляются
- [ ] Выбывание работает корректно
- [ ] Переход к следующему раунду работает

---

## Следующие шаги

После успешного тестирования:
1. Протестируйте быструю игру (нужно несколько аккаунтов)
2. Протестируйте голосование
3. Протестируйте досрочную победу в финале
4. Протестируйте тай-брейк (при равенстве)
