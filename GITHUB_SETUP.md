# Инструкция по выгрузке на GitHub (приватный репозиторий)

## 🔒 Создание приватного репозитория

### Шаг 1: Создать репозиторий на GitHub

1. Зайдите на [GitHub.com](https://github.com)
2. Нажмите **"+"** → **"New repository"**
3. Заполните:
   - **Repository name:** `trivia-bot` (или любое другое имя)
   - **Description:** "Telegram bot trivia game Brain Survivor"
   - **Visibility:** ⚠️ **Выберите "Private"** (приватный)
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license"
4. Нажмите **"Create repository"**

### Шаг 2: Инициализация Git в проекте

```bash
# Перейти в директорию проекта
cd C:\projects\megagames\trivia-bot

# Инициализировать Git (если еще не инициализирован)
git init

# Добавить все файлы
git add .

# Сделать первый коммит
git commit -m "Initial commit: Trivia Bot Brain Survivor"
```

### Шаг 3: Подключение к GitHub

```bash
# Добавить remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/trivia-bot.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/trivia-bot.git

# Отправить код на GitHub
git branch -M main
git push -u origin main
```

### Шаг 4: Проверка приватности

1. Зайдите на страницу репозитория: `https://github.com/YOUR_USERNAME/trivia-bot`
2. В правом верхнем углу должно быть написано **"Private"** 🔒
3. Если написано "Public" → см. раздел "Изменить настройки приватности"

---

## 🔐 Изменить настройки приватности

Если репозиторий уже создан как публичный:

1. Зайдите на страницу репозитория
2. Нажмите **"Settings"** (вкладка вверху)
3. Прокрутите вниз до раздела **"Danger Zone"**
4. Нажмите **"Change visibility"**
5. Выберите **"Make private"**
6. Подтвердите действие

---

## 📝 Что НЕ попадет в репозиторий

Благодаря `.gitignore`, следующие файлы **НЕ** будут загружены:

- ✅ `.env` - ваши секретные ключи
- ✅ `logs/` - логи
- ✅ `venv/` - виртуальное окружение
- ✅ `__pycache__/` - кэш Python
- ✅ `*.pyc` - скомпилированные файлы
- ✅ Файлы ТЗ (`.odt`, `.txt` с ТЗ) - можно оставить или удалить

---

## 🚨 Важные моменты безопасности

### ✅ Обязательно проверьте перед push:

1. **`.env` файл НЕ должен быть в репозитории:**
   ```bash
   # Проверить, что .env в .gitignore
   cat .gitignore | grep .env
   
   # Проверить, что .env не добавлен в Git
   git status | grep .env
   ```

2. **Токен бота должен быть только в `.env`:**
   - НЕ коммитьте файлы с токенами
   - НЕ добавляйте токены в код

3. **Пароли БД должны быть только в `.env`**

### 🔍 Проверка перед коммитом:

```bash
# Посмотреть, что будет закоммичено
git status

# Если видите .env в списке - НЕ коммитьте!
# Удалите из индекса:
git reset HEAD .env
```

---

## 📋 Полная последовательность команд

```bash
# 1. Перейти в проект
cd C:\projects\megagames\trivia-bot

# 2. Инициализировать Git (если нужно)
git init

# 3. Проверить .gitignore
cat .gitignore

# 4. Проверить статус
git status

# 5. Добавить файлы
git add .

# 6. Проверить, что .env НЕ добавлен
git status | grep .env
# Если ничего не вывело - хорошо!

# 7. Сделать коммит
git commit -m "Initial commit: Trivia Bot Brain Survivor"

# 8. Добавить remote (замените YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/trivia-bot.git

# 9. Переименовать ветку в main (если нужно)
git branch -M main

# 10. Отправить на GitHub
git push -u origin main
```

---

## 🔑 Аутентификация на GitHub

### Вариант 1: Personal Access Token (рекомендуется)

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите права: `repo` (полный доступ к репозиториям)
4. Скопируйте токен
5. При `git push` используйте токен как пароль

### Вариант 2: SSH ключи

```bash
# Сгенерировать SSH ключ (если еще нет)
ssh-keygen -t ed25519 -C "your_email@example.com"

# Добавить ключ в ssh-agent
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# Скопировать публичный ключ
cat ~/.ssh/id_ed25519.pub

# Добавить ключ на GitHub:
# Settings → SSH and GPG keys → New SSH key
```

---

## 🛡️ Дополнительная безопасность

### 1. Добавить .env.example в репозиторий

`.env.example` уже есть и содержит примеры без реальных значений - это безопасно.

### 2. Использовать GitHub Secrets (для CI/CD)

Если планируете использовать GitHub Actions:
- Settings → Secrets and variables → Actions
- Добавьте секреты там, а не в коде

### 3. Ограничить доступ

В настройках репозитория:
- Settings → Collaborators
- Добавьте только нужных людей
- Убедитесь, что репозиторий Private

---

## ✅ Чек-лист перед push

- [ ] Репозиторий создан как **Private**
- [ ] `.env` файл в `.gitignore`
- [ ] `.env` не добавлен в Git (`git status` не показывает .env)
- [ ] Токены и пароли только в `.env`
- [ ] `.env.example` есть в репозитории (без реальных значений)
- [ ] Логи не коммитятся (`logs/` в `.gitignore`)
- [ ] Виртуальное окружение не коммитится (`venv/` в `.gitignore`)

---

## 🚀 Быстрая команда (все в одной строке)

```bash
cd C:\projects\megagames\trivia-bot && git init && git add . && git commit -m "Initial commit" && git branch -M main && echo "Теперь добавьте remote: git remote add origin https://github.com/YOUR_USERNAME/trivia-bot.git && git push -u origin main"
```

---

## 📞 Если что-то пошло не так

### Проблема: "Repository not found"
- Проверьте правильность URL
- Проверьте, что репозиторий существует
- Проверьте права доступа

### Проблема: "Permission denied"
- Проверьте аутентификацию (токен или SSH)
- Проверьте права доступа к репозиторию

### Проблема: ".env попал в репозиторий"
```bash
# Удалить из Git (но оставить локально)
git rm --cached .env

# Добавить в .gitignore (если еще нет)
echo ".env" >> .gitignore

# Закоммитить изменения
git add .gitignore
git commit -m "Remove .env from repository"

# Отправить изменения
git push
```

---

## 📚 Полезные ссылки

- [GitHub: Creating a private repository](https://docs.github.com/en/repositories/creating-and-managing-repositories/creating-a-new-repository)
- [GitHub: Managing repository visibility](https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/managing-repository-settings/managing-the-visibility-of-your-repository)
- [GitHub: Personal Access Tokens](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token)
