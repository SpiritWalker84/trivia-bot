# 🚀 Как запустить для тестирования

## Шаг 1: Подготовка (один раз)

```bash
# Активировать виртуальное окружение
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Создать таблицы БД
python scripts/create_tables.py

# Добавить тестовые данные (темы, вопросы, боты)
python scripts/add_test_data.py
```

## Шаг 2: Запуск сервисов (3 терминала)

### Терминал 1 - Telegram Bot
```bash
source venv/bin/activate
python main.py
```

### Терминал 2 - Celery Worker
```bash
source venv/bin/activate
celery -A tasks.celery_app worker --loglevel=info
```

### Терминал 3 - Celery Beat (для периодических задач)
```bash
source venv/bin/activate
celery -A tasks.celery_app beat --loglevel=info
```

## Шаг 3: Тестирование

### A. Базовый тест бота
1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. ✅ Должно появиться меню

### B. Создать тестовую игру
```bash
# Узнайте ваш Telegram ID (отправьте /start, ID будет в логах бота)
# Или используйте @userinfobot в Telegram

python scripts/create_test_game.py --telegram-id YOUR_TELEGRAM_ID
```

**В Telegram:**
- ✅ Должно прийти: "🎮 Игра начинается!"
- ✅ Должен прийти первый вопрос с кнопками A, B, C, D
- Нажмите на кнопку → должно появиться уведомление
- Через 20 секунд → автоматически придет следующий вопрос

## Проверка логов

```bash
# Логи бота
tail -f logs/trivia_bot.log

# Логи Celery
tail -f logs/celery_worker.log
```

## Проверка статуса

```bash
python scripts/check_status.py
```

---

## ⚠️ Важно перед запуском

1. **Создайте `.env` файл:**
   ```bash
   cp .env.example .env
   nano .env  # Заполните TELEGRAM_BOT_TOKEN и другие параметры
   ```

2. **Убедитесь, что PostgreSQL и Redis запущены:**
   ```bash
   # PostgreSQL
   sudo systemctl start postgresql
   
   # Redis
   sudo systemctl start redis-server
   ```

---

## 📋 Быстрая команда (все в одной строке)

```bash
# Подготовка
python scripts/create_tables.py && python scripts/add_test_data.py

# Затем запустите 3 терминала с командами выше
```
