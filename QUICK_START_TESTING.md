# Быстрый старт тестирования

## 🚀 Быстрая инструкция (5 минут)

### Шаг 1: Подготовка (2 минуты)

```bash
# 1. Активировать окружение
source venv/bin/activate

# 2. Создать таблицы БД
python scripts/create_tables.py

# 3. Добавить тестовые данные
python scripts/add_test_data.py

# 4. Проверить статус
python scripts/check_status.py
```

**Ожидаемый результат:**
```
SYSTEM STATUS
==================================================
👥 Users: Total: 11, Real users: 1, Bots: 10
📚 Themes: 5
❓ Questions: Total: 12, Approved: 12
🎮 Games: Total: 0, Active: 0, Finished: 0
```

### Шаг 2: Запуск сервисов (1 минута)

**Терминал 1 - Бот:**
```bash
python main.py
```

**Терминал 2 - Celery Worker:**
```bash
celery -A tasks.celery_app worker --loglevel=info
```

**Терминал 3 - Celery Beat:**
```bash
celery -A tasks.celery_app beat --loglevel=info
```

### Шаг 3: Тестирование (2 минуты)

#### Тест 1: Базовый функционал
1. Откройте Telegram → найдите бота
2. Отправьте `/start`
3. ✅ Должно появиться приветствие и меню

#### Тест 2: Создание тестовой игры
**В новом терминале:**
```bash
# Узнайте ваш Telegram ID (отправьте /start боту, ID будет в логах)
# Или используйте @userinfobot в Telegram

python scripts/create_test_game.py --telegram-id YOUR_TELEGRAM_ID
```

**В Telegram:**
1. ✅ Должно прийти сообщение "🎮 Игра начинается!"
2. ✅ Должен прийти первый вопрос с кнопками A, B, C, D
3. Нажмите на любую кнопку
4. ✅ Должно появиться "✅ Правильно!" или "❌ Неправильно"

#### Тест 3: Проверка автоматики
1. Подождите 20 секунд
2. ✅ Должен автоматически прийти следующий вопрос
3. ✅ Боты должны отвечать автоматически (проверьте логи Celery)

#### Тест 4: Результаты раунда
1. Подождите, пока пройдут все 10 вопросов
2. ✅ Должны прийти результаты раунда с таблицей
3. ✅ Должен быть указан выбывший игрок

---

## 🔍 Проверка работы

### Проверка логов

**Логи бота:**
```bash
tail -f logs/trivia_bot.log
```

**Логи Celery:**
```bash
tail -f logs/celery_worker.log
```

### Проверка статуса

```bash
python scripts/check_status.py
```

### Проверка БД напрямую

```bash
psql -U trivia_user -d trivia_bot

# Проверить игры
SELECT id, status, current_round FROM games;

# Проверить ответы
SELECT COUNT(*) FROM answers;

# Проверить игроков
SELECT user_id, is_bot, total_score FROM game_players WHERE game_id = 1;
```

---

## 🐛 Решение проблем

### Проблема: "User not found"
**Решение:** Отправьте `/start` боту сначала

### Проблема: "No questions found"
**Решение:** Запустите `python scripts/add_test_data.py`

### Проблема: "Celery not working"
**Решение:** 
1. Проверьте Redis: `redis-cli ping`
2. Перезапустите Celery worker

### Проблема: "Questions not sending"
**Решение:**
1. Проверьте логи Celery worker
2. Убедитесь, что Celery beat запущен
3. Проверьте, что бот запущен

---

## 📊 Что проверить

- [ ] Бот отвечает на команды
- [ ] Вопросы отправляются в ЛС
- [ ] Inline-кнопки работают
- [ ] Ответы обрабатываются
- [ ] Боты отвечают автоматически
- [ ] Следующие вопросы отправляются
- [ ] Результаты раунда отправляются
- [ ] Выбывание работает корректно

---

## 🎯 Следующие тесты

После успешного базового тестирования:
1. Протестируйте быструю игру (нужно несколько аккаунтов)
2. Протестируйте голосование
3. Протестируйте досрочную победу в финале
4. Протестируйте тай-брейк (при равенстве)
